<?xml version="1.0" encoding="UTF-8"?>
<metadata>
	<layout title="COM_BOILERPLATE_CATEGORY_VIEW_DEFAULT_TITLE"
			option="View">
		<message>
			<![CDATA[COM_BOILERPLATE_CATEGORY_VIEW_DEFAULT_DESC]]>
		</message>
	</layout>

	<!-- Add fields to the request variables for the layout. -->
	<fields name="request">
		<fieldset name="request">
			<field name="id"
				   type="sql"
				   label="COM_BOILERPLATE_CATEGORY_FIELD_LABEL"
				   description="COM_BOILERPLATE_CATEGORY_FIELD_DESC"
				   query="SELECT id, title FROM #__categories WHERE extension = 'com_boilerplate' ORDER BY id ASC"
				   key_field="id"
				   value_field="title"
				   required="true">
				<option value="">COM_BOILERPLATE_SELECT_CATEGORY</option>
			</field>
		</fieldset>
	</fields>

	<fields name="params">
		<fieldset name="basic">
			<!-- Define your basic params for the view here -->
		</fieldset>
	</fields>
</metadata>